[user_mapping]

# Admins (no agent_id = can see all data)
[user_mapping.alexa]
# Admin - no agent_id needed

[user_mapping.jordi]
# Team Lead - no agent_id needed

[user_mapping.maria]
# QA - no agent_id needed

[user_mapping.rafael]
# QA - no agent_id needed

# Agents (set agent_id to match the Agent field in your PDF data)
# Agent IDs are now in format: bpagent01, bpagent02, bpagent03, etc.
# (extracted from "BPO Agent 01", "BPO Agent 02" in PDF content)
[user_mapping.jesus]
agent_id = "bpagent01"  # BPO Agent 01 - Jesus Godinez

[user_mapping.gerardo]
agent_id = "bpagent02"  # BPO Agent 02 - Gerardo Fernandez

[user_mapping.edgar]
agent_id = "bpagent03"  # BPO Agent 03 - Edgar Cardoza

[user_mapping.osiris]
agent_id = "bpagent04"  # BPO Agent 04 - Osiris García

[user_mapping.daniela]
agent_id = "bpagent06"  # BPO Agent 06 - Daniela Maruri

[user_mapping.yasmin]
agent_id = "bpagent07"  # BPO Agent 07 - Yasmin Contreras

[user_mapping.moises]
agent_id = "bpagent08"  # BPO Agent 08 - Moises Ramirez

[user_mapping.marcos]
agent_id = "bpagent09"  # BPO Agent 09 - Marcos Hernandez

[user_mapping.angel]
agent_id = "bpagent10"  # BPO Agent 10 - Angel Tellez

# Valence admins (no agent_id = can see all data)
[user_mapping.chloe]
# Admin - no agent_id needed

[user_mapping.shannon]
# Admin - no agent_id needed
